@model IEnumerable<HobbyLobbyMVC.Models.Truck>

@{
    ViewBag.Title = "TruckNumber";
}

<h2>Trucks</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@

<div class="col-md-4">
    <p><a class="btn btn-secondary" href="https://localhost:44327/trucks/Create">Create new  &raquo;</a></p>
</div>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Capacity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TruckNumber)
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Capacity)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TruckNumber)
    </td>

    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.TruckNumber }) |
        @Html.ActionLink("Details", "Details", new { id = item.TruckNumber }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.TruckNumber })
    </td>
</tr>
}

</table>
